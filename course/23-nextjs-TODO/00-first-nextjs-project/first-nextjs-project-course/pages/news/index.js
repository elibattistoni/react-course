//| this will be loaded if in the domain we have /news
//| so NB here the filename "news" will be used as a path name in the URL

const NewsPage = (props) => {
  return <h1>The News Page</h1>;
};

export default NewsPage;
